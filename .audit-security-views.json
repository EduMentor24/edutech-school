{
  "project_id": "nnshioowwniursnozicg",
  "query": "SELECT c.relname AS view_name, c.reloptions, pg_get_viewdef(c.oid, true) AS definition FROM pg_class c JOIN pg_namespace n ON n.oid = c.relnamespace WHERE n.nspname = 'public' AND c.relkind = 'v' AND c.relname IN ('edutech_leaderboard', 'schoolci_grade_summary') ORDER BY c.relname LIMIT 10;"
}
