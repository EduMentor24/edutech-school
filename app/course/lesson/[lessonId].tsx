import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { useLocalSearchParams, useRouter } from "expo-router";
import { useCallback, useEffect, useMemo, useState } from "react";
import { Pressable, ScrollView, StyleSheet, Text, View } from "react-native";

import { AppScreen } from "@/components/edutech/app-screen";
import { CourseEmpty, CourseError, CourseLoading } from "@/components/edutech/course-feedback";
import { LessonMarkdown } from "@/components/edutech/lesson-markdown";
import { LessonStatusBadge } from "@/components/edutech/learning-progress";
import { PageHeader } from "@/components/edutech/page-header";
import { useSupabaseAuth } from "@/lib/auth/supabase-auth-provider";
import { CourseLesson, getLesson, getLessonsForChapter } from "@/lib/courses/course-service";
import { useEduTheme } from "@/lib/edutech/theme-context";
import { ExerciseCatalogItem, getExerciseCatalog } from "@/lib/exercises/exercise-service";
import { completeLesson, getLearningProgress, progressForLesson, recordLessonView, setLessonFavorite, type LessonProgressStatus } from "@/lib/progress/learning-progress-service";
import { QuizCatalogItem, getQuizCatalog } from "@/lib/quizzes/quiz-service";

export default function LessonReaderScreen() {
  const { lessonId } = useLocalSearchParams<{ lessonId: string }>(); const router = useRouter(); const { colors } = useEduTheme(); const { profile } = useSupabaseAuth();
  const [lesson, setLesson] = useState<CourseLesson | null>(null); const [chapterLessons, setChapterLessons] = useState<CourseLesson[]>([]); const [lessonExercises, setLessonExercises] = useState<ExerciseCatalogItem[]>([]); const [lessonQuizzes, setLessonQuizzes] = useState<QuizCatalogItem[]>([]); const [status, setStatus] = useState<LessonProgressStatus>("not_started"); const [isFavorite, setIsFavorite] = useState(false); const [trackingError, setTrackingError] = useState<string | null>(null); const [saving, setSaving] = useState(false); const [loading, setLoading] = useState(true); const [error, setError] = useState<string | null>(null); const styles = useMemo(() => createStyles(colors), [colors]);

  const load = useCallback(async () => {
    if (!lessonId) return;
    setLoading(true); setError(null); setTrackingError(null);
    try {
      const nextLesson = await getLesson(lessonId); const nextChapterLessons = nextLesson ? await getLessonsForChapter(nextLesson.chapterId) : [];
      setLesson(nextLesson); setChapterLessons(nextChapterLessons); setLessonExercises([]); setLessonQuizzes([]);
      if (nextLesson && profile?.role === "student") {
        try {
          const dashboard = await getLearningProgress(); const knownProgress = progressForLesson(dashboard, nextLesson.id);
          setStatus(knownProgress?.status ?? "not_started"); setIsFavorite(knownProgress?.isFavorite ?? false); const savedProgress = await recordLessonView(nextLesson.id); setStatus(savedProgress.status);
        } catch (cause) { setTrackingError(cause instanceof Error ? cause.message : "La consultation n’a pas pu être enregistrée."); }
        try { const [exercises, quizzes] = await Promise.all([getExerciseCatalog({ lessonId: nextLesson.id }), getQuizCatalog({ lessonId: nextLesson.id })]); setLessonExercises(exercises); setLessonQuizzes(quizzes); } catch (cause) { setTrackingError((current) => current ?? (cause instanceof Error ? cause.message : "Les activités associées n’ont pas pu être chargées.")); }
      } else { setStatus("not_started"); setIsFavorite(false); }
    } catch (cause) { setError(cause instanceof Error ? cause.message : "Une erreur inattendue est survenue."); } finally { setLoading(false); }
  }, [lessonId, profile?.role]);

  useEffect(() => { void load(); }, [load]);

  const handleComplete = useCallback(async () => {
    if (!lesson || profile?.role !== "student") return;
    setSaving(true); setTrackingError(null);
    try { const savedProgress = await completeLesson(lesson.id); setStatus(savedProgress.status); } catch (cause) { setTrackingError(cause instanceof Error ? cause.message : "La complétion n’a pas pu être enregistrée."); } finally { setSaving(false); }
  }, [lesson, profile?.role]);

  const handleFavorite = useCallback(async () => {
    if (!lesson || profile?.role !== "student") return;
    setSaving(true); setTrackingError(null);
    try { setIsFavorite(await setLessonFavorite(lesson.id, !isFavorite)); } catch (cause) { setTrackingError(cause instanceof Error ? cause.message : "Le favori n’a pas pu être mis à jour."); } finally { setSaving(false); }
  }, [isFavorite, lesson, profile?.role]);

  if (loading) return <AppScreen><CourseLoading label="Chargement de la leçon" /></AppScreen>;
  if (error) return <AppScreen><CourseError message={error} onRetry={() => void load()} /></AppScreen>;
  if (!lesson) return <AppScreen><CourseEmpty title="Leçon indisponible" description="Cette leçon n’est pas accessible avec votre profil scolaire actuel." /></AppScreen>;
  const currentIndex = chapterLessons.findIndex((item) => item.id === lesson.id); const previous = currentIndex > 0 ? chapterLessons[currentIndex - 1] : null; const next = currentIndex >= 0 ? chapterLessons[currentIndex + 1] : null; const canTrack = profile?.role === "student";
  return <AppScreen withPadding={false}><ScrollView contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}><View style={styles.inner}><PageHeader title={lesson.title} subtitle={lesson.description || "Lecture de la leçon."} back />{canTrack ? <View style={styles.progressCard}><View style={styles.statusRow}><LessonStatusBadge status={status} /><Pressable accessibilityRole="button" accessibilityLabel={isFavorite ? "Retirer des favoris" : "Ajouter aux favoris"} disabled={saving} onPress={() => void handleFavorite()} style={({ pressed }) => [styles.iconButton, pressed && !saving && styles.pressed]}><MaterialIcons name={isFavorite ? "bookmark" : "bookmark-border"} size={21} color={colors.primary} /></Pressable></View>{status === "completed" ? <Text style={styles.completedText}>Cette leçon est enregistrée comme terminée.</Text> : <Pressable accessibilityRole="button" disabled={saving} onPress={() => void handleComplete()} style={({ pressed }) => [styles.completeButton, saving && styles.disabled, pressed && !saving && styles.pressed]}><MaterialIcons name="check-circle-outline" size={19} color={colors.surface} /><Text style={styles.completeLabel}>{saving ? "Enregistrement…" : "Marquer comme terminée"}</Text></Pressable>}{trackingError ? <Text style={styles.trackingError}>{trackingError}</Text> : null}</View> : null}<View style={styles.reader}>{lesson.content ? <LessonMarkdown content={lesson.content} /> : <Text style={styles.emptyContent}>Le contenu de cette leçon sera ajouté prochainement.</Text>}</View>{canTrack ? <><View style={styles.exerciseCard}><View style={styles.exerciseHeader}><View style={styles.exerciseIcon}><MaterialIcons name="edit-note" size={20} color={colors.primary} /></View><View style={styles.exerciseCopy}><Text style={styles.exerciseTitle}>Exercices associés</Text><Text style={styles.exerciseDescription}>{lessonExercises.length ? `${lessonExercises.length} exercice${lessonExercises.length > 1 ? "s" : ""} disponible${lessonExercises.length > 1 ? "s" : ""} pour cette leçon.` : "Aucun exercice associé n’est disponible pour le moment."}</Text></View></View>{lessonExercises.length ? <Pressable accessibilityRole="button" onPress={() => router.push({ pathname: "/(tabs)/exercises", params: { lessonId: lesson.id } })} style={({ pressed }) => [styles.exerciseButton, pressed && styles.pressed]}><Text style={styles.exerciseLabel}>S’entraîner sur la leçon</Text><MaterialIcons name="arrow-forward" size={18} color={colors.primary} /></Pressable> : null}</View><View style={styles.quizCard}><View style={styles.exerciseHeader}><View style={styles.exerciseIcon}><MaterialIcons name="quiz" size={20} color={colors.primary} /></View><View style={styles.exerciseCopy}><Text style={styles.exerciseTitle}>Quiz associés</Text><Text style={styles.exerciseDescription}>{lessonQuizzes.length ? `${lessonQuizzes.length} quiz disponible${lessonQuizzes.length > 1 ? "s" : ""} pour cette leçon.` : "Aucun quiz associé n’est disponible pour le moment."}</Text></View></View>{lessonQuizzes.length ? <Pressable accessibilityRole="button" onPress={() => router.push({ pathname: "/(tabs)/quizzes", params: { lessonId: lesson.id } })} style={({ pressed }) => [styles.exerciseButton, pressed && styles.pressed]}><Text style={styles.exerciseLabel}>Évaluer mes acquis</Text><MaterialIcons name="arrow-forward" size={18} color={colors.primary} /></Pressable> : null}</View></> : null}<View style={styles.navigation}><NavButton direction="previous" label={previous ? "Leçon précédente" : "Début du chapitre"} disabled={!previous} onPress={() => previous && router.replace(`/course/lesson/${previous.id}`)} /><NavButton direction="next" label={next ? "Leçon suivante" : "Fin du chapitre"} disabled={!next} onPress={() => next && router.replace(`/course/lesson/${next.id}`)} /></View></View></ScrollView></AppScreen>;
}

function NavButton({ direction, label, disabled, onPress }: { direction: "previous" | "next"; label: string; disabled: boolean; onPress: () => void }) { const { colors } = useEduTheme(); const styles = useMemo(() => navStyles(colors), [colors]); const icon = direction === "previous" ? "chevron-left" : "chevron-right"; return <Pressable disabled={disabled} onPress={onPress} style={({ pressed }) => [styles.button, disabled && styles.disabled, pressed && !disabled && styles.pressed]}><MaterialIcons name={icon} size={20} color={disabled ? colors.muted : colors.primary} /><Text style={[styles.label, { color: disabled ? colors.muted : colors.primary }]}>{label}</Text></Pressable>; }

const createStyles = (colors: ReturnType<typeof useEduTheme>["colors"]) => StyleSheet.create({ content: { paddingTop: 18, paddingBottom: 30 }, inner: { paddingHorizontal: 20 }, progressCard: { marginTop: 18, padding: 14, backgroundColor: colors.surface, borderRadius: 18, borderWidth: 1, borderColor: colors.border }, statusRow: { flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, iconButton: { minHeight: 38, minWidth: 38, alignItems: "center", justifyContent: "center", borderRadius: 12, backgroundColor: colors.primarySoft }, completeButton: { minHeight: 44, marginTop: 12, borderRadius: 13, backgroundColor: colors.primary, flexDirection: "row", justifyContent: "center", alignItems: "center", gap: 7, paddingHorizontal: 14 }, completeLabel: { color: colors.surface, fontSize: 13, fontWeight: "900" }, completedText: { color: colors.success, fontSize: 13, lineHeight: 18, fontWeight: "800", marginTop: 10 }, trackingError: { color: colors.error, fontSize: 12, lineHeight: 17, marginTop: 10 }, reader: { marginTop: 23, backgroundColor: colors.surface, borderRadius: 20, borderWidth: 1, borderColor: colors.border, padding: 18 }, emptyContent: { color: colors.text, fontSize: 16, lineHeight: 26 }, exerciseCard: { marginTop: 18, gap: 13, padding: 15, borderRadius: 18, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.surface }, quizCard: { marginTop: 12, gap: 13, padding: 15, borderRadius: 18, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.surface }, exerciseHeader: { flexDirection: "row", gap: 11 }, exerciseIcon: { width: 38, height: 38, borderRadius: 12, alignItems: "center", justifyContent: "center", backgroundColor: colors.primarySoft }, exerciseCopy: { flex: 1 }, exerciseTitle: { color: colors.text, fontSize: 14, fontWeight: "900" }, exerciseDescription: { color: colors.muted, fontSize: 12, lineHeight: 18, marginTop: 3 }, exerciseButton: { minHeight: 41, paddingHorizontal: 12, borderRadius: 12, backgroundColor: colors.primarySoft, flexDirection: "row", alignItems: "center", justifyContent: "space-between" }, exerciseLabel: { color: colors.primary, fontSize: 12, fontWeight: "900" }, navigation: { flexDirection: "row", gap: 10, marginTop: 18 }, disabled: { opacity: 0.55 }, pressed: { opacity: 0.72 } });
const navStyles = (colors: ReturnType<typeof useEduTheme>["colors"]) => StyleSheet.create({ button: { flex: 1, minHeight: 48, borderRadius: 15, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.surface, flexDirection: "row", alignItems: "center", justifyContent: "center", gap: 5, paddingHorizontal: 9 }, disabled: { backgroundColor: colors.background }, pressed: { opacity: 0.72 }, label: { fontSize: 12, fontWeight: "800", textAlign: "center" } });
