import { ReactNode, useMemo } from "react";
import { ActivityIndicator, Pressable, StyleSheet, Text, ViewStyle } from "react-native";
import { useEduTheme } from "@/lib/edutech/theme-context";
type PrimaryButtonProps = { label: string; onPress: () => void; variant?: "primary" | "secondary" | "text" | "danger"; icon?: ReactNode; disabled?: boolean; loading?: boolean; style?: ViewStyle };
export function PrimaryButton({ label, onPress, variant = "primary", icon, disabled = false, loading = false, style }: PrimaryButtonProps) { const { colors } = useEduTheme(); const styles = useMemo(() => createStyles(colors), [colors]); const buttonStyle = variant === "primary" ? styles.primary : variant === "danger" ? styles.danger : variant === "secondary" ? styles.secondary : styles.textButton; const labelStyle = variant === "primary" || variant === "danger" ? styles.inverseLabel : variant === "text" ? styles.textLabel : styles.secondaryLabel; return <Pressable accessibilityRole="button" accessibilityLabel={label} disabled={disabled || loading} onPress={onPress} style={({ pressed }) => [styles.base, buttonStyle, style, (disabled || loading) && styles.disabled, pressed && styles.pressed]}>{loading ? <ActivityIndicator color={variant === "primary" || variant === "danger" ? colors.surface : colors.primary} /> : icon}<Text style={labelStyle}>{label}</Text></Pressable>; }
const createStyles = (colors: ReturnType<typeof useEduTheme>["colors"]) => StyleSheet.create({ base: { minHeight: 50, borderRadius: 15, flexDirection: "row", alignItems: "center", justifyContent: "center", gap: 8, paddingHorizontal: 18 }, primary: { backgroundColor: colors.primary }, secondary: { backgroundColor: colors.primarySoft, borderWidth: 1, borderColor: colors.border }, textButton: { backgroundColor: "transparent" }, danger: { backgroundColor: colors.error }, inverseLabel: { color: colors.surface, fontSize: 15, lineHeight: 19, fontWeight: "800" }, secondaryLabel: { color: colors.primary, fontSize: 15, lineHeight: 19, fontWeight: "800" }, textLabel: { color: colors.primary, fontSize: 14, lineHeight: 19, fontWeight: "800" }, disabled: { opacity: 0.56 }, pressed: { transform: [{ scale: 0.98 }], opacity: 0.9 } });
