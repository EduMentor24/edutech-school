import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { useMemo } from "react";
import { StyleSheet, Text, View } from "react-native";
import { useEduTheme } from "@/lib/edutech/theme-context";
export function BrandMark({ compact = false }: { compact?: boolean }) { const { colors } = useEduTheme(); const styles = useMemo(() => createStyles(colors.primary, colors.primaryDark, colors.surface), [colors.primary, colors.primaryDark, colors.surface]); return <View style={styles.row}><View style={compact ? styles.markCompact : styles.mark}><MaterialIcons name="menu-book" size={compact ? 22 : 30} color={colors.surface} /></View>{!compact && <View><Text style={styles.name}>EduTech</Text><Text style={styles.school}>SCHOOL</Text></View>}</View>; }
const createStyles = (primary: string, primaryDark: string, surface: string) => StyleSheet.create({ row: { flexDirection: "row", alignItems: "center", gap: 10 }, mark: { width: 54, height: 54, borderRadius: 17, backgroundColor: primary, alignItems: "center", justifyContent: "center", elevation: 4 }, markCompact: { width: 38, height: 38, borderRadius: 13, backgroundColor: primary, alignItems: "center", justifyContent: "center" }, name: { color: primaryDark, fontSize: 21, fontWeight: "800", lineHeight: 25, letterSpacing: -0.4 }, school: { color: primary, fontSize: 10, fontWeight: "800", lineHeight: 13, letterSpacing: 2.1 } });
