import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { ComponentProps, ReactNode, useMemo } from "react";
import { Pressable, StyleSheet, Text, View } from "react-native";
import { useEduTheme } from "@/lib/edutech/theme-context";
type IconName = ComponentProps<typeof MaterialIcons>["name"];
type SettingsRowProps = { icon: IconName; title: string; subtitle?: string; onPress: () => void; accessory?: ReactNode; danger?: boolean };
export function SettingsRow({ icon, title, subtitle, onPress, accessory, danger = false }: SettingsRowProps) { const { colors } = useEduTheme(); const styles = useMemo(() => createStyles(colors, danger), [colors, danger]); return <Pressable accessibilityRole="button" accessibilityLabel={title} onPress={onPress} style={({ pressed }) => [styles.row, pressed && styles.pressed]}><View style={styles.iconBox}><MaterialIcons name={icon} size={21} color={danger ? colors.error : colors.primary} /></View><View style={styles.copy}><Text style={styles.title}>{title}</Text>{subtitle ? <Text style={styles.subtitle}>{subtitle}</Text> : null}</View>{accessory ?? <MaterialIcons name="chevron-right" size={22} color={colors.muted} />}</Pressable>; }
const createStyles = (colors: ReturnType<typeof useEduTheme>["colors"], danger: boolean) => StyleSheet.create({ row: { minHeight: 68, flexDirection: "row", alignItems: "center", gap: 12, paddingVertical: 10, paddingHorizontal: 12, backgroundColor: colors.surface, borderBottomWidth: 1, borderBottomColor: colors.border }, iconBox: { width: 38, height: 38, borderRadius: 13, backgroundColor: danger ? colors.warningSoft : colors.primarySoft, alignItems: "center", justifyContent: "center" }, copy: { flex: 1 }, title: { color: danger ? colors.error : colors.text, fontSize: 15, lineHeight: 20, fontWeight: "700" }, subtitle: { color: colors.muted, fontSize: 12, lineHeight: 17, marginTop: 1 }, pressed: { opacity: 0.68 } });
